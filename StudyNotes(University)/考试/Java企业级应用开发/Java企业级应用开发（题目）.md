# Java企业级应用开发（答案）

> **说明**：本卷严格按照复习大纲顺序列出，所有 `[   ]` 均代表一个不可遗漏的考点（包含手写笔记的全部细节）。

## 二、 Spring Boot 核心知识点

**【启动类注解】**
1. `@SpringBootApplication` 是复合注解，包含 `[   ]`（标识配置类）、`[   ]`（扫描组件）和 `[   ]`（启用自动配置）。
2. 【手写笔记】Spring Boot 3 的三大特性是：`[   ]`、`[   ]`、`[   ]`。如何理解自动配置？根据项目中引入的 `[   ]` 自动配置组件环境 Bean。
3. 开启定时任务功能的注解是 `[   ]`。
4. 专用于明确标识 Spring Boot 配置类（与 @Configuration 作用相同）的注解是 `[   ]`。
5. 允许开发者自定义 MVC 配置而不覆盖自动配置的注解是 `[   ]`。
6. 开启注解驱动的事务管理，允许使用 `@Transactional` 的注解是 `[   ]`。

**【配置文件】**
7. 配置文件支持 YAML 和 Properties，其中适合层次化配置的是 `[   ]` 格式。
8. 完整配置优先级排序：`[   ]` > `[   ]` > `[   ]` > `application.yml / application.properties`。
9. 激活特定多环境配置的属性是 `[   ]`。
10. 将配置文件属性绑定到 Java 对象的注解是 `[   ]`，【手写笔记】它可以读取配置文件的 `[   ]` 类型属性。
11. 在 yml 中使用 `[   ]` 分隔符可以定义多个 profile 的配置。

**【内嵌服务器】**
12. 【手写笔记】什么是内嵌服务器？集成了 Tomcat 等服务器，无需 `[   ]`，直接运行 `[   ]` 启动 Web 项目。
13. 默认服务器是 Tomcat，还支持选择 `[   ]` 和 `[   ]`。修改端口号的配置属性是 `[   ]`。
14. yml 中配置 SSL 证书可实现 `[   ]` 协议；通过简单配置启用 `[   ]` 协议可在高延迟网络下提升性能。

**【全局异常处理 & 请求处理】**
15. 全局异常处理共有 `[   ]` 个核心注解，类上通常标注 `[   ]` 或 `[   ]`，方法上标注 `[   ]`。
16. 接收【由前端传来】的 HTTP 请求体中 JSON 数据的注解是 `[   ]`。
17. `@RestController` 是 `[   ]` 和 `[   ]` 的组合注解；【手写笔记】自动注入使用的注解是 `[   ]`。
18. 获取 URL 中的路径变量用 `[   ]`；获取请求参数用 `[   ]`；获取请求头信息用 `[   ]`。
19. 【手写笔记】整个 Spring Boot 项目的静态资源存放在 `[   ]` 目录；测试类注解是 `[   ]`。

**【事务管理】**
20. 事务传播行为有七种，【手写笔记】默认的是 `[   ]`；事务隔离级别有 `[   ]` 种。
21. 防止长时间运行占用资源，需设置 `@Transactional` 的 `[   ]` 属性；提升查询性能可设置 `[   ]` 属性。

**【异步任务 & 全局响应】**
22. 标记方法异步执行的注解是 `[   ]`，需配合开启注解 `[   ]` 使用。【手写笔记】其特点是独立线程执行，不阻塞 `[   ]`，提升响应速度。
23. 配置自定义线程池可通过实现 `[   ]` 接口或定义 `ThreadPoolTaskExecutor` Bean。处理异步异常需实现 `[   ]` 接口。
24. 全局响应处理用于统一添加响应头或封装响应体，除了 @RestControllerAdvice 还需要 `[   ]` 接口。
25. 解决跨域问题可通过全局配置或使用 `[   ]` 注解。

**【热部署】**
26. 【手写笔记】热部署底层核心特征模块是 `[   ]`。开启它需要配置 `spring.devtools.restart.enabled=[   ]`。
27. 配合 `[   ]` 插件可实现浏览器自动刷新。为减少不必要的重启，可配置排除 `[   ]`。
28. 【手写笔记】Spring Boot 整合 Redis 连接池常用的两种实现是：`[   ]` 和 `[   ]`。

---

## 三、 MyBatis-Plus 核心知识点

**【主键与表映射】**
29. 【手写笔记】MP 核心优势是：无需编写基础 `[   ]`，大幅减少重复代码。
30. `@TableId` 指定主键生成策略（type），【手写笔记】四种策略是：`[   ]`、`[   ]`、`[   ]`、手动输入。
31. 指定实体类对应表名用 `[   ]`；指定映射关系及填充策略用 `[   ]`；Oracle 使用序列自增用 `[   ]`。

**【CRUD 与条件构造器】**
32. 包含基础 CRUD 方法的核心接口是 `[   ]`；【手写笔记】查询记录数的方法是 `[   ]`。
33. 查询所有数据调用的方法是 `[   ]`；`selectOne` 若结果不唯一会抛出 `[   ]`；批量插入效率高的方法是 `[   ]`。
34. 只需部分字段以减少传输量时，适用返回 Map 集合的方法 `[   ]`。
35. 【手写笔记】条件构造器共有 `[   ]` 个，其最大好处是无需手写 SQL，防止 `[   ]`。支持链式调用且防误拼的是 `[   ]` 和 `[   ]`。
36. 嵌套条件可通过 `[   ]`、`[   ]` 构建；调用数据库函数使用 `[   ]` 方法。
37. 【手写笔记】MP 有多种插件，除了分页、乐观锁、逻辑删除外，笔记中还提到了 `[   ]` 插件、数据权限插件、`[   ]` 插件等。

**【分页插件、乐观锁、逻辑删除】**
38. MP 必须配置分页插件才能分页。配置时需注入 `[   ]` 并配置 `PaginationInnerInterceptor`。传参和返回都需要使用 `[   ]` 对象。
39. 【手写笔记】实现乐观锁的 3 个步骤：① 实体类字段加 `[   ]` 注解；② 配置 `[   ]`；③ 数据库设置 version 字段。作用是控制并发，更新时版本号 `[   ]`。
40. 【手写笔记】逻辑删除需配置 3 个要素：① 实体类字段注解 `[   ]`；② `[   ]` 配置；③ 数据库字段。默认值 `[   ]` 表示未删除，`[   ]` 表示已删除。

---

## 四、 Redis 核心知识点

**【场景与数据结构】**
41. 【手写笔记】整合 Redis 的专门用于操作 String 类型的是 `[   ]`；通用核心操作类是 `[   ]`。
42. 【手写笔记】Redis 的适用场景有 5 个：①`[   ]` ②`[   ]`(防重复提交) ③权限限制 ④`[   ]` ⑤学号类型等。
43. 数据结构对应：最常用/计数器选 `[   ]`；消息队列(先进先出)选 `[   ]`；去重运算选 `[   ]`；排行榜/范围选 `[   ]`；对象属性(支持字典级操作)选 `[   ]`；基数统计选 `[   ]`；LBS位置服务选 `[   ]`。

**【命令与机制】**
44. 【手写笔记】整合序列化的目的是让数据 `[   ]`。3 种序列化方式：`[   ]`、`[   ]`、`[   ]`。推荐（4.0前）使用的是 `[   ]` 序列化器。
45. 发布消息的命令是 `[   ]`；List 入队/出队是 LPUSH/RPOP，【手写笔记】还有 `[   ]`。
46. 查看 key 的剩余过期时间命令是 `[   ]`；清空所有数据库数据的危险命令是 `[   ]`；增量迭代不阻塞主线程的命令是 `[   ]`。
47. RDB 是基于 `[   ]` 的持久化；AOF 是记录 `[   ]` 的持久化，【手写笔记】重启时重新执行，安全性较 `[   ]`。两者优点结合称为 `[   ]`。

**【分布式锁】**
48. 实现分布式锁的基础核心命令是 `[   ]`；【手写笔记】用来解决分布式 `[   ]` 问题。
49. 防止死锁必须设置 `[   ]`；长时间任务未完成可通过 `[   ]` 防止锁释放。更可靠的客户端推荐使用 `[   ]`。

---

## 五、 Spring Cache 与定时任务

**【Spring Cache】**
50. 【手写笔记：重点首要词】查询操作注解是 `[   ]`；更新操作注解是 `[   ]`；清空操作注解是 `[   ]`；统一配置缓存属性的类级别注解是 `[   ]`。
51. 缓存 key 的生成方式有：默认生成、`[   ]` 表达式自定义、手动指定。
52. 【手写笔记】缓存失效三大原因：① `[   ]` ② `[   ]` ③ `[   ]`。
53. 解决缓存穿透用 `[   ]`；解决缓存雪崩用 `[   ]`（或高可用集群/多级缓存）；解决缓存击穿用 `[   ]`（或设永久有效）。

**【定时任务】**
54. 【手写笔记】开启定时任务需组合 `@Scheduled` 和 `[   ]` 两个注解。
55. 固定频率执行 `@Scheduled(fixedDelay=5000)` 的精确含义是：`[   ]`。
56. Cron 表达式的 6 个槽位顺序是：`[   ]`。
57. 【手写笔记】举例：`"0 0 1 * * ?"` 代表 `[   ]`；`"0/5 * * * * ?"` 代表 `[   ]`。
58. 定时任务的三种实现方式：① 基于 `@Scheduled` 注解；② 结合 `[   ]` 接口；③ 使用第三方库如 `[   ]`（支持分布式集群和持久化）。

**【补充步骤】**
59. 截图 9 补充的 SpringBoot 整合 Redis 的 5 个步骤：① 环境准备 ② 配置 `pom.xml` 核心依赖 ③ 配置 `application.yml` ④ `[   ]` (解决序列化乱码) ⑤ `[   ]` (避免重复代码)。