# Java企业级应用开发（题目）

## 第一部分：填空题（基于大纲打印文字内容）

### 一、 Spring Boot 核心知识点
**启动类与配置**
1. `@SpringBootApplication` 是一个复合注解，包含 `[   ]`（标识该类为配置类）、`[   ]`（自动扫描组件）和 `[   ]`（启用自动配置）三个核心注解。
2. 用于开启 Spring Boot 应用定时任务功能的注解是 `[   ]`。
3. 允许开发者自定义 MVC 配置而不覆盖 Spring Boot 自动配置的注解是 `[   ]`。
4. 开启注解驱动的事务管理的注解是 `[   ]`。
5. 配置优先级顺序为：`[   ]` > 外部配置文件 > `[   ]` > `application.yml / application.properties`。
6. 多环境配置中，使用 `[   ]` 属性来激活特定的环境配置；在 `application.yml` 中可以使用 `[   ]` 分隔符来定义多个 profile。

**内嵌服务器与异常处理**
7. Spring Boot 默认使用 `[   ]` 作为内嵌服务器，此外还支持 `[   ]` 和 Undertow。
8. 想要实现 HTTPS 协议，需要在 application.yml 中进行 `[   ]` 配置；提升高延迟网络下 Web 应用性能可启用 `[   ]` 协议。
9. 用于定义全局异常处理器的类级别注解是 `[   ]` 或 `[   ]`，方法级别需要结合 `[   ]` 注解。

**请求处理与事务、异步**
10. 将 HTTP 请求体中的 JSON 数据自动绑定到方法参数上的注解是 `[   ]`。
11. `@RestController` 是 `[   ]` 和 `[   ]` 的组合注解。
12. 获取 URL 中的路径变量使用 `[   ]`；获取请求参数使用 `[   ]`；获取请求头信息使用 `[   ]`。
13. 事务传播行为共有 `[   ]` 种，事务隔离级别共有 `[   ]` 种。
14. 防止长时间运行的事务占用资源可设置 `@Transactional` 的 `[   ]` 属性；提升查询性能可设置 `[   ]` 属性。
15. 配置异步线程池可以通过实现 `[   ]` 接口；处理异步任务异常需实现 `[   ]` 接口。
16. 全局响应处理用于统一添加响应头或封装响应体，需结合 `[   ]` 或 `@ResponseBodyAdvice` 接口。跨域问题可通过 `[   ]` 注解解决。

### 二、 MyBatis-Plus 核心知识点
**映射与 CRUD**
17. 指定实体类对应的数据库表名的注解是 `[   ]`；标记主键字段的注解是 `[   ]`。
18. 实现逻辑删除功能的实体类注解是 `[   ]`。对于 Oracle 数据库指定序列的注解是 `[   ]`。
19. 指定实体类字段与数据库表字段映射关系（含填充策略）的注解是 `[   ]`。
20. `selectList(null)` 用于查询所有数据；`selectOne` 查询结果必须唯一，否则会抛出 `[   ]`；只需要部分字段时适用返回 Map 集合的方法 `[   ]`。
21. 条件构造器中，支持链式调用的是 `[   ]` 和 LambdaUpdateWrapper；在条件中调用数据库函数需使用 `[   ]` 方法。

**插件与特性**
22. 启用分页功能需注入 `MybatisPlusInterceptor` 并配置 `[   ]`。查询时需传入并返回 `[   ]` 对象。
23. 逻辑删除字段默认值为 `[   ]` 表示未删除，`[   ]` 表示已删除。

### 三、 Redis 核心知识点
**数据结构与命令**
24. 适合存储消息队列的数据结构是 `[   ]`；无序不重复数据集合是 `[   ]`；有序不重复集合是 `[   ]`；用于基数统计的是 `[   ]`；用于地理位置的是 `[   ]`。
25. 删除指定键及其对应值的命令是 `[   ]`；判断键是否存在的命令是 `[   ]`；清空所有数据库数据的危险命令是 `[   ]`。
26. 批量设置/获取多个键值的命令是 `[   ]`；避免阻塞主线程的增量式迭代命令是 `[   ]`。

**持久化、锁与序列化**
27. 基于快照的持久化方式是 `[   ]`；记录执行命令的持久化方式是 `[   ]`。
28. 结合上述两者优点的机制是 `[   ]`；减少存储空间可启用 `[   ]` 压缩，减少文件体积可配置 `[   ]` 重写。
29. 防止死锁可为锁设置 `[   ]` 或使用 Redlock 算法；长时间任务防止锁释放需进行 `[   ]`。
30. 推荐支持 JSON 格式的序列化器是 `[   ]`，此外还有 JdkSerializationRedisSerializer 和 StringRedisSerializer。

### 四、 Spring Cache 与定时任务
31. 用于查询并缓存数据的注解是 `[   ]`；用于更新缓存且不影响方法执行的注解是 `[   ]`；用于清空指定缓存的注解是 `[   ]`。
32. 组合多个缓存操作的注解是 `[   ]`；统一配置缓存名称的类注解是 `[   ]`。
33. 缓存穿透防护可在缓存中设置 `[   ]` 或使用互斥锁；缓存击穿对热点数据考虑使用 `[   ]` 或互斥锁。
34. `@Scheduled` 注解支持 cron 表达式、`[   ]`、`[   ]` 属性；设置首次执行任务的延迟时间需使用 `[   ]` 属性，指定时区使用 `[   ]` 属性。

---

## 第二部分：名词解释/简答题（基于手写笔记补充内容）

1. **Spring Boot 3 特性**：请列举 Spring Boot 3 的三大底层核心特性，并解释什么是“自动配置”。
2. **@ConfigurationProperties 的特殊能力**：除了普通属性，该注解还能读取配置文件中的什么特定类型的属性？
3. **内嵌服务器**：什么是 Spring Boot 的内嵌服务器？如何修改它的默认端口号？
4. **Spring Boot 测试与静态资源**：Spring Boot 的测试类需要标注什么注解？整个项目的静态资源通常放置在什么目录下？
5. **@Async 异步任务的作用**：使用独立线程执行方法有什么明显的好处？
6. **Spring Boot 热部署**：实现热部署的底层核心特征行为依赖哪个模块？其 Maven artifactId 是什么？
7. **Redis 连接池**：Spring Boot 整合 Redis 时，常用的两种连接池技术是什么？
8. **MyBatis-Plus 核心优势**：请简述 MyBatis-Plus 框架最核心的优势是什么？
9. **MyBatis-Plus 主键策略**：@TableId 注解支持的主键生成策略主要有哪四种？
10. **BaseMapper 核心扩展**：MyBatis-Plus 的 BaseMapper 接口中，用于查询记录数的方法是什么？用于批量插入操作的方法是什么？
11. **MyBatis-Plus 条件构造器的作用**：使用条件构造器（如 LambdaQueryWrapper）的最大好处是什么？
12. **MyBatis-Plus 常见插件**：除了分页、乐观锁、逻辑删除插件外，笔记中还列举了哪些 MyBatis-Plus 插件？（写出至少三种）
13. **乐观锁及其实现步骤**：什么是乐观锁？在 MyBatis-Plus 中实现乐观锁需要哪三个必不可少的步骤？
14. **逻辑删除的必备条件**：要完整实现 MyBatis-Plus 的逻辑删除，需要具备哪三个配置条件？
15. **Redis 适用场景**：请列举 Redis 的五个典型应用场景。
16. **Redis 整合与核心操作**：专门用于操作 String 类型和通用的 Redis 核心操作类分别是什么？整合 Redis 序列化的目的是什么？有哪三种常见序列化方式？
17. **Redis 其他常用命令**：查看 key 的剩余过期时间的命令是什么？发布消息的命令是什么？操作 List 结构的三个常用命令（除了 LPUSH 和 RPOP）还有什么？
18. **AOF 持久化特点**：AOF 持久化的恢复机制是什么？相比 RDB 其安全性如何？
19. **Redis 分布式锁**：利用 Redis 实现互斥锁的核心基础命令是什么？其主要解决什么场景下的问题？
20. **Spring Cache 失效原因**：请列举导致 Spring Cache 缓存失效的三大原因。
21. **Spring Boot 定时任务参数**：请解释 `@Scheduled(fixedDelay=5000)` 的精确执行逻辑；并写出 Cron 表达式 `0 0 1 * * ?` 和 `0/5 * * * * ?` 的具体含义。
22. **定时任务的三种实现方式**：除了基于 @Scheduled 注解，还有哪两种实现定时任务的方式？
23. **全局核心注解（零散知识点整合）**：@RequestBody 接收的数据通常由谁传来？在控制器中用于自动注入 Bean 的注解是什么？事务传播行为的默认值是什么？